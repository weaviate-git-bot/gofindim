import tw, {styled} from "twin.macro";



const PrimaryButton = styled('button')(() => [
tw`bg-slate-500 rounded-md p-1 flex w-fit place-self-center align-middle mt-2 place-self-end text-sm`
])

export default PrimaryButton;
